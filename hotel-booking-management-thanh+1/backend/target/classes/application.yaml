spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_booking_management?useSSL=false&serverTimezone=UTC
    username: root
    password: huynhmm123A
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
